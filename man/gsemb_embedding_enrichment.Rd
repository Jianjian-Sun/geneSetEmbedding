% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{gsemb_embedding_enrichment}
\alias{gsemb_embedding_enrichment}
\title{Enrichment analysis using gene‑set Gaussian embeddings}
\usage{
gsemb_embedding_enrichment(
  gene_stats,
  x,
  sets = NULL,
  gene_sets = NULL,
  score = c("loglik", "neg_mahalanobis"),
  temperature = 1,
  nperm = 1000,
  alternative = c("two.sided", "greater", "less"),
  seed = 1,
  eps = 1e-08,
  top_genes = 30
)
}
\arguments{
\item{gene_stats}{Named numeric vector of gene‑level statistics.}

\item{x}{A `gsemb_embedding` object.}

\item{sets}{Optional character vector of set IDs to test.}

\item{gene_sets}{Named list of original gene‑set members (used for set‑size
reporting and for restricting the candidate genes when `sets` is supplied).}

\item{score}{Scoring method: `"loglik"` (full Gaussian log‑likelihood) or
`"neg_mahalanobis"` (negative squared Mahalanobis distance).}

\item{temperature}{Soft‑max temperature for converting scores to weights.}

\item{nperm}{Number of permutations for null distribution.}

\item{alternative}{Alternative hypothesis for permutation test.}

\item{seed}{Random seed for permutations.}

\item{eps}{Small constant added to variances for numerical stability.}

\item{top_genes}{Number of top‑weighted genes to report in `core_enrichment`.}
}
\value{
A data.frame with columns: `ID`, `ES` (enrichment score), `z` (z‑score),
  `pvalue`, `p.adjust`, `setSize`, `core_enrichment`.
}
\description{
Given a vector of gene‑level statistics (e.g., log‑fold‑changes, p‑values),
compute enrichment scores for each gene set based on the likelihood of the
statistics under the set’s Gaussian embedding. Permutation‑based p‑values
and multiple‑testing adjusted q‑values are provided.
}
\examples{
\dontrun{
# Assuming you have a fitted gsemb_embedding object named 'fit'
# (see example in gsemb_fit)
# Simulate some gene‑level statistics
all_genes <- rownames(fit$gene_embedding)
stats <- rnorm(length(all_genes))
names(stats) <- all_genes

# Run enrichment analysis
enrich <- gsemb_embedding_enrichment(
  gene_stats = stats,
  x = fit,
  gene_sets = list(
    SET1 = sample(all_genes, 10),
    SET2 = sample(all_genes, 8)
  ),
  score = "loglik",
  nperm = 100,
  alternative = "greater"
)

# View top enriched sets
head(enrich[order(enrich$pvalue), ])
}
}
